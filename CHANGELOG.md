# Changelog

## [Unreleased]

- App shell scaffold: window + tray (close-to-tray, start-minimized, run-at-login),
  settings (JSON snapshot via app-kit), signed auto-update, `starlume://` deep link,
  single-instance, device-token auth plumbing (browser handoff, keyring storage),
  module registry skeleton, typed IPC (tauri-specta), CI + publish workflows.
